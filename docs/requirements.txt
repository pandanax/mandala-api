# Зависимости ТОЛЬКО для просмотрщика документации (mkdocs).
# Изолированы от рантайма бота: не входят в pyproject/uv.lock и в прод-образ.
# Ставятся в отдельное окружение через scripts/docs-serve.sh.
mkdocs>=1.6
mkdocs-material>=9.5
